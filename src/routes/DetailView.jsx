import RecipeDetail from "../Components/RecipeDetail";

// this route shows the detail view content by accessing the RecipeDetail component directly
const DetailView = () => {
  return (
    <div>
      <RecipeDetail />
    </div>
  );
};

export default DetailView;